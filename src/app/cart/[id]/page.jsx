export default function CartByUser({ params }) {
    return (
        <div>
            <h1>Cart By User</h1>
            <p>Aca estaria la vista del carrito del usuario con el id: {params.id}</p>
        </div>
    )
}